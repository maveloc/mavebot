# mavebot
Este es el código fuente del bot de Discord MaveBot.

---

### ⚙️ Puesta a Punto del Bot (Instrucciones Completas)

**1. Requisitos Previos**
Asegúrate de tener instalado: **Python 3.8+**, **Git** y el **Token de tu Bot de Discord** (obtenido desde el [Portal de Desarrolladores](https://discord.com/developers/applications/)).

**2. Configuración del Entorno Virtual**
Debes crear y activar un entorno virtual (`venv`) para aislar las dependencias del proyecto.

```bash
# Crear el entorno virtual
python3 -m venv venv

# Activar el entorno virtual (Linux/macOS - Zsh/Bash)
source venv/bin/activate

# crea alpsctr.py donde alojes lo siguiente

token = "clave secreta discord"